server {
    listen 8080;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This line is vital for React Router
        try_files $uri $uri/ /index.html;
    }
}
